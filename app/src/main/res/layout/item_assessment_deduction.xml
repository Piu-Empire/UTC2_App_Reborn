<?xml version="1.0" encoding="utf-8"?>
<!--
    item_assessment_deduction.xml  (fix_v7)
    Thêm tvReadonlyDeduction — hiện khi cột != SV đánh giá (readonly, nền xám).
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#FFF5F5">

    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="#FFCCCC"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:gravity="center_vertical"
        android:minHeight="52dp">

        <!-- Nội dung trừ điểm -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginEnd="10dp">

            <TextView
                android:id="@+id/tvDeductionTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="13sp"
                android:textColor="@color/assessment_deduction_red"
                android:lineSpacingMultiplier="1.3"/>

            <TextView
                android:id="@+id/tvPenalty"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="#FF6666"
                android:layout_marginTop="2dp"
                android:visibility="gone"/>

        </LinearLayout>

        <!-- Editable dropdown (SV đánh giá) -->
        <AutoCompleteTextView
            android:id="@+id/actvDeductionScore"
            android:layout_width="80dp"
            android:layout_height="44dp"
            android:background="@drawable/bg_score_box"
            android:backgroundTint="#FFF5F5"
            android:paddingStart="0dp"
            android:paddingEnd="0dp"
            android:gravity="center"
            android:inputType="none"
            android:textSize="14sp"
            android:textColor="@color/assessment_deduction_red"
            android:textStyle="bold"
            android:maxLines="1"
            android:focusable="true"
            android:focusableInTouchMode="false"
            android:cursorVisible="false"
            android:visibility="visible"/>

        <!-- Readonly (TapThe / Khoa / Truong) -->
        <TextView
            android:id="@+id/tvReadonlyDeduction"
            android:layout_width="80dp"
            android:layout_height="44dp"
            android:background="@drawable/bg_score_box"
            android:backgroundTint="@color/assessment_readonly_bg"
            android:gravity="center"
            android:textSize="14sp"
            android:textColor="#999999"
            android:textStyle="bold"
            android:visibility="gone"/>

    </LinearLayout>

</LinearLayout>